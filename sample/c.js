// This JavaScript code is not used in the current example,
// as the button's alert is directly in the HTML.
// But it shows how you would potentially handle events in JS.
//
// function handleClick() {
//   alert("Button Clicked!");
// }
//
// document.querySelector("button").addEventListener("click", handleClick);